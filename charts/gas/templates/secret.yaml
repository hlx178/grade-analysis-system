apiVersion: v1
kind: Secret
metadata:
  name: {{ include "gas.fullname" . }}-secrets
type: Opaque
data:
  {{- range $k, $v := .Values.app.secretEnv }}
  {{ $k | lower }}: {{ $v | b64enc | quote }}
  {{- end }}

